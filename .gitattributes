# 让 .bat 在任何人克隆后都是 CRLF 换行，不依赖本机 core.autocrlf 设置。
# 原因：cmd.exe 对 LF-only 的批处理会在解析带括号的多行块（if (...)/for (...)/do (...)）时出错。
*.bat text eol=crlf

# 本文件自身保持 LF，避免 autocrlf 反复改写
.gitattributes text eol=lf
