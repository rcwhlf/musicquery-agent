# MusicQuery Agent 答辩详案（单人版）

> 配套：MusicQuery-Agent-答辩.pptx（10 页）。本详案 = 逐页讲稿 + 演示操作 + 问答全解 + 应急预案。
> 用法：讲稿念熟到"能脱稿说大意"即可，不要背诵字句；Q&A 部分重点背"答案要点"。

---

## 一、逐页讲稿（总计约 10 分钟）

### P1 封面（30 秒）

> 各位老师好。我们小组答辩的项目是 MusicQuery Agent——基于自然语言交互的音乐作品库智能查询系统。一句话概括：用户用中文提问，系统自动把问题转成 SQL、查询 MySQL 数据库，再用不超过 50 字的自然语言回答。整个项目里，**数据库设计是核心，Agent 是交互层**。下面我从数据库设计、Agent 实现、现场演示三部分汇报。

### P2 系统定位（1 分钟）

> 系统是一条完整闭环：用户提问先过**意图判断**这道守门员，无关问题直接礼貌拒绝；相关问题由大模型依据真实表结构生成 SQL，经过**三道安全防线**校验后执行，执行失败还能把报错回传给大模型自动修正；最后把查询结果翻译成 50 字以内的中文回答。技术栈：MySQL 8.4 加 Python 生态——PyMySQL、Streamlit，模型用智谱 GLM。

### P3 数据库设计 · ER 图（2.5 分钟）★ 重头

> 数据库一共 6 张表：artists、albums、songs、users 四张维度表，加一张事实表 play_records——**一条记录代表一次播放**，查询路径像星星一样从事实表向四周发散，这就是星型模型：按歌曲、按用户、按时间统计都从事实表出发，路径清晰。第六张 query_logs 是 Agent 自己的日志表，和业务解耦。
> 三个设计点：**第一，全部 InnoDB 加外键约束**，引用完整性由数据库本身保证，不靠应用程序自觉；**第二，职责单一**，业务和日志分开；**第三，有一处受控冗余**——songs 表同时持有 album_id 和 artist_id，下一页详细说。

### P4 设计决策（1.5 分钟）

> 四个决策：**受控冗余**——songs 同时存专辑 ID 和歌手 ID，按歌手查歌不用绕道专辑表做二次 JOIN，代价是一致性靠导入层保证，这是有意识的取舍；**整体满足三范式**，非主属性完全、直接依赖主键，名字类的信息都不重复存；**ENUM 枚举**把 album_type 限定在三种取值，数据库层就挡住脏数据；**事实表主键用 BIGINT**，它增长最快，INT 上限约 21 亿，给规模化留余量。右侧是测试数据规模：50 歌手、100 专辑、500 歌曲、200 用户、5000 条播放，和任务书一致。

### P5 索引设计（1.5 分钟）★ 有实证

> 四个索引全部对应高频查询。重点讲两个复合索引：**songs(genre, release_year)** 的顺序遵循最左前缀原则——genre 是等值过滤、选择性强，放左边，这样单独查 genre、genre 加年份都能用上这个索引；**play_records(song_id, play_time)** 先等值再范围，服务单曲播放量统计。
> 更有说服力的是实测：我们在这台 MySQL 8.4 上跑了 EXPLAIN——三个代表性查询分别命中了设计的索引；最典型的是 song_id 加时间的查询，**优化器在两个候选索引里自己选了复合索引，而且显示 Using index，说明是覆盖索引，连回表都省了**。

### P6 Agent 四步流程（2 分钟）

> 第一步意图判断，无关问题一步拦截：先走一条**确定性关键词快速通道**（命中音乐领域词或库中流派取值就直接放行，不依赖模型、不消耗调用），未命中再交给一个小 Prompt 让模型只回答 YES 或 NO。第二步 SQL 生成，把建表语句全文和字段说明写进提示词，附加严格规则。第三步是核心：先安全校验再执行，执行出错就把**报错原文回传**给大模型修正，修正后重新走一遍安全校验，最多重试两次。第四步把结果转成 JSON，生成 50 字以内的中文结论。
> 底部是工程细节：提示词里的表结构是**运行时直接读 schema.sql 的**——改了表结构，提示词自动跟着变，单一数据源，不会出现"提示词和真实表结构对不上"的经典事故。

### P7 安全设计（1.5 分钟）

> 安全是我们最花心思的部分，三道防线：**语句白名单**只放行 SELECT 开头；**关键词黑名单**用正则拦住 DROP、DELETE、UPDATE、INSERT、ALTER、TRUNCATE；**全程留痕**，query_logs 把每次请求连同是否被拦截都记下来。右边是失败修正闭环：出错回传大模型，修正后重新校验再执行。举个例子：让它"删除所有播放记录"，DELETE 一命中黑名单直接被拒，而且日志里能看到这条 is_success 为 0 的记录。

### P8 现场演示（2.5 分钟）★ 操作 + 讲解

> 下面现场演示。（打开演示问题清单）
> ① 先演示流派筛选：上方选「摇滚」，问"播放量最高的前 5 首歌"——大家看生成的 SQL，WHERE 里带了 genre='摇滚'，筛选直接写进 SQL——结果第一名《My Universe》。
> ② 只输入"周杰伦"三个字——光杆歌手名也能默认查出他的 12 首歌。
> ③ 问一个无关问题："今天天气怎么样？"——被意图判断拦下，提示与音乐数据库无关。
> ④ 再来一个安全测试："删除所有播放记录"——DELETE 命中黑名单被拒。大家看右侧栏历史记录，这条被拦截的请求 is_success 是 0，**全程可追溯**；历史还支持单条删除与一键清空。

### P9 数据设计亮点（1 分钟）

> 测试数据的曲库部分我们换成了真实公开数据（50 位真实歌手、100 张真实专辑、500 首真实曲目，从《范特西》到《Thriller》）；播放行为属于隐私数据无法拿到真实日志，按**对数正态长尾分布**仿真：头部 BTS《Euphoria》132 次、长尾歌曲个位数。最初均匀随机时前五名全挤在二十次左右并列，排名没有信息量，改成长尾后区分明显。数据量仍是任务书要求的 5000 条。

### P10 总结（1 分钟）

> 项目交付了 6 张表的完整设计、4 个经 EXPLAIN 实证的索引、三道安全防线、最多两次的失败自动重试。改进方向也想清楚了：应用账号只授 SELECT 权限把防线下沉到数据库层、用 few-shot 示例库提升复杂问句准确率、加缓存和连接池。我的汇报到此结束，谢谢各位老师。

---

## 二、演示操作清单（答辩前一晚 + 当天各跑一遍）

1. 开机检查：`netstat -ano | findstr 3306` 有输出（MySQL 在跑）；没有就双击 `start_mysql.bat`
2. 启动应用：项目目录 `streamlit run app.py`
3. 网络检查：先试跑一条示例问题，确认大模型接口通
4. 兜底：手机热点随时可切；U 盘里备一组成功运行截图；PPT 存本地 + 邮箱各一份
5. 演示顺序固定：正常查询 ×2 → 无关问题 → 安全拦截 → 指侧边栏日志

---

## 三、Q&A 十题全解（背"要点"，不是背句子）

**Q1 songs 表为什么同时有 album_id 和 artist_id？**
受控冗余。按歌手查歌可以直连，不用绕道专辑表二次 JOIN；一致性由导入层保证（歌曲归属专辑对应的歌手），生产上还有外键约束兜底。

**Q2 复合索引为什么 genre 放前面？**
最左前缀原则：必须从最左列开始匹配。genre 是等值过滤、选择性高放左边；单独查 genre、genre+年份都能用到，单独查年份用不上——这是有意识的取舍。

**Q3 怎么防止 LLM 删库？**
三道防线：SELECT 白名单、危险词正则黑名单、全程日志留痕。改进方向：给应用分配只有 SELECT 权限的数据库账号，把防线下沉到数据库层。

**Q4 LLM 生成的 SQL 报错了怎么办？**
把报错原文和原 SQL 回传给模型自动修正，修正后重新过安全校验，最多重试 2 次；每次尝试都写 query_logs，包括失败的。

**Q5 满足三范式吗？**
满足：非主属性完全、直接依赖主键，歌手名这类信息只存 ID。唯一例外是受控冗余，并且能说出理由和兜底——比"全部符合"更能体现设计能力。

**Q6 为什么事实表用 BIGINT？**
play_records 增长最快，INT 无符号上限约 42 亿（有符号 21 亿），事实表最容易触及，BIGINT 一次到位。

**Q7 并发怎么处理？**
短连接模式，每次查询新建连接用完即关，无共享状态；改进方向是连接池。

**Q8 大模型输出不稳定怎么办？**
temperature 设 0 保证确定性；Prompt 里写死严格规则（只允许 SELECT、字段必须来自 DDL）；代码里剥离代码块和多余解释；执行失败自动修正；全部留痕便于分析 badcase。关键路径上加确定性校验，不把所有判断都交给模型。

**Q9 项目的局限性？**
诚实答：依赖单一模型供应商、没有结果缓存、不支持多步分析类问题。对应改进：示例库结构化、语义缓存、只读账号、连接池。

**Q10 数据是真实的吗？5000 条够吗？**
Faker 按任务书数量生成：50/100/500/200/5000，随机种子固定可复现；分布用对数正态模拟真实热度长尾——量符合要求，分布有设计，排名有区分度。

---

## 四、单人答辩技巧

- **时间**：每讲完一页看一眼表；演示超时就砍掉第②个正常查询，保两个特殊场景
- **被打断**：老师中途提问是好事，答完说"谢谢老师，我继续"
- **不会答**：诚实 + 给方向——"这一点我们目前实现到这里，如果要改进，我的思路是……"（比硬编强十倍）
- **AI 辅助披露**（2026 年大概率被问）："我们用 AI 辅助编码提效，但架构设计、数据库建模、安全方案是我们定的，关键代码都做过验收和改动实验——比如 EXPLAIN 实测索引、故意改坏 Prompt 看生成质量变化。"（公开仓库的提交历史就是证据）
- **忘词**：低头看 PPT 页面本身，每一页的文字就是提词器

## 五、备用回答（被追问时再用，不必主动讲）

**被问到"你们用的模型是不是偏弱 / 为什么用免费模型？"**

是，我们用的是智谱免费档的 **glm-4-flash**，这是有意的取舍：调用成本为零，把预算全留给服务器，对课程项目和小微场景是合理的。

代价我们很清楚，而且是**实测出来的**：它在"一共有多少首歌？""流行歌曲有多少首？"这类**没有点名实体的泛化问句**上，会把相关问题误判为无关——同一批问题仅语序不同，判定就可能相反（"一共有多少首流行歌曲？"被拦，"有多少首流行歌曲？"却放行）。

所以意图判断我们没有单纯交给模型，关键路径上额外加了一条**确定性关键词快速通道**：命中音乐领域词（歌、专辑、流派、播放、用户…）或库中实际存在的流派取值（流行、摇滚、民谣…）就直接放行，不受模型波动影响，也不消耗 API 调用；未命中才交给模型判断。另外也往提示词里补了 few-shot 示例。

一句话：**把可靠性建在代码里，而不是指望模型变强。** 要提升准确率，改 `.env` 换成 glm-5.3-flash 即可，代码一行都不用动。

**被追问"那为什么不用更强的模型？"**

预算取向。免费档对课程项目刚好够用；而且系统做了模型无关的适配（OpenAI 兼容接口），换模型只改配置、不改代码——这是设计时就留好的余地。

## 六、答辩前检查清单

- [ ] PPT 封面姓名/团队/日期填好
- [ ] 讲稿过 3 遍，Q&A 要点能脱口而出
- [ ] 演示电脑：MySQL 启动、Streamlit 启动、网络测试通过
- [ ] 手机热点已测试
- [ ] U 盘备份：PPT + 成功截图
- [ ] 提前 20 分钟到场
